return {
  "stevearc/dressing.nvim",
  ---@module 'dressing.config'
  opts = {
    input = {
      -- enable vim motions on floating inputs like LSP rename
      insert_only = false,
      -- start in normal mode
      start_in_insert = false,
    },
  },
}
