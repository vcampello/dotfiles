# $schema=https://json.schemastore.org/lazygit.json
git:
  diffRenderers:
    - colorArg: always
      command: delta --dark --paging=never --line-numbers
gui:
  theme:
    selectedLineBgColor:
      - '#333333'
